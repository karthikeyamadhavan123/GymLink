//all comment based ts here 
// comments/types/types.ts
export interface CommentPresenterProps {
  comment: {
    storyNumber: string;
    description: string;
    image: string;
    id: string;
  };
}
